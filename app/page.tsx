"use client"
import MainPage from "./components/mainPage";

export default function Home() {
  // const number = Math.floor(Math.random() *10) + 1
  // if(number % 2 === 0) {
  //   throw new Error('The random number is even.')
  // }
  return (
      <MainPage/>
  );
}
